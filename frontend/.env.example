# Copy to .env. Vite only exposes vars prefixed with VITE_.

# Same-origin in dev via the Vite proxy (see vite.config.ts).
VITE_API_BASE=/api

# Stripe publishable key (TEST mode)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...
