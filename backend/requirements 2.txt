# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- DB / ORM / migrations ---
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3        # Postgres (prod)
# SQLite ships with Python for dev

# --- Config / validation ---
pydantic==2.10.4
pydantic-settings==2.7.0

# --- Auth ---
pyjwt==2.10.1
argon2-cffi==23.1.0

# --- Services ---
stripe==11.4.1
boto3==1.35.90                # Cloudflare R2 (S3-compatible)
resend==2.5.1

# --- Dev / test ---
pytest==8.3.4
httpx==0.28.1                 # FastAPI TestClient / async client
ruff==0.8.6
